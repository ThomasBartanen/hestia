import { TabWidget, LineEdit, GridBox, ComboBox, SpinBox, StandardButton, ListView, Button, StandardTableView, ScrollView, VerticalBox } from "std-widgets.slint";
import { Validation, IdType } from "validation.slint";
import { BufferLine, TableText, TableHeader, TableItemActionIcons, CreationActionIcons, TableItem } from "custom_widgets.slint";
import { PeriodType, ExpenseData, ExpenseInput, MessageType } from "globals.slint";

enum ExpenseMenuState {totals, view}

export component MainExpenseView inherits Rectangle {
    Rectangle {
        Text {
            text: "Number of Expenses: " + ExpenseData.expenses.length;
        }
    }

    visualizations := Rectangle {
        VerticalLayout {
            HorizontalLayout {
                period-selection := ComboBox {
                    max-height: 10px;
                    property <PeriodType> period-selected;
                    model: [
                        "Current Day",
                        "Current Week",
                        "Current Month",
                        "YTD",
                        "Custom"
                    ];
                }
            }
            Rectangle {
                GridLayout {
                    Rectangle {
                        col: 0;
                        row: 0;
                        Text {
                            text: "";
                        }
                    }
                    Rectangle {
                        col: 1;
                        row: 0;
                        Text {
                            text: "";
                        }
                    }
                    Rectangle {
                        col: 0;
                        row: 1;
                        Text {
                            text: "";
                        }
                    }
                    Rectangle {
                        col: 1;
                        row: 1;
                        Text {
                            text: "Total: " + ExpenseData.get-totals(period-selection.period-selected);
                        }
                    }
                }
            }
        }
        Rectangle {}

        Rectangle {}
    }
}

export component ViewExpenseTotalsMenu inherits Rectangle {
    in-out property <ExpenseInput> selected-expense;
    in-out property <string> expense-subtype-string;
    in-out property <MessageType> expense-action;
    y: 10px;
    x: 20px;
    padding: 10px;
    VerticalLayout {
        x: 30px;
        titles := HorizontalLayout {
            TableHeader { 
                width: 40px;
                text: "ID";
            }
            TableHeader { 
                width: 40px;
                text: "Property";
            }
            TableHeader { 
                width: 100px;
                text: "Date";
            }
            TableHeader { 
                width: 150px;
                text: "Description";
            }
            TableHeader { 
                width: 150px;
                text: "Expense Type";
            }
            TableHeader { 
                width: 150px;
                text: "Expense Subtype";
            }
            TableHeader { 
                width: 70px;
                text: "Amount";
            }
        }
        BufferLine { 
            buffer: 10px;
            line-thickness: 3px;
        }
        ScrollView {
            VerticalLayout {
                alignment: LayoutAlignment.start;
                preferred-width: 0in;
                x: 0cm;
                padding: 0;
                for x in ExpenseData.expenses: expense-line := HorizontalLayout {
                    expense-id := TableItem {
                        width: 40px;
                        text: x.id;
                    }
                    expense-prop-id := TableItem {
                        width: 40px;
                        text: x.prop-id;
                        //TODO: Translate prop id to property name. Maybe use combobox for the edit section
                    }
                    expense-date := TableItem { 
                        width: 100px;
                        text: x.date;
                    }
                    expense-description := TableItem { 
                        width: 150px;
                        text: x.description;
                    }
                    TableItem {
                        width: 150px;
                        text: x.expense-type;
                    }
                    TableItem {
                        width: 150px;
                        text: x.expense-subtype;               
                    }
                    expense-amount := TableItem { 
                        width: 70px;
                        text: x.amount;
                    }
                    expense-action-icons := TableItemActionIcons {
                        width: 70px;
                        edit => {
                            selected-expense = x;
                            new-expense.current-use = MessageType.update;

                            new-expense-id.edit-text = x.id;
                            new-expense-prop-id.edit-text = x.prop-id;
                            new-expense-description.edit-text = x.description;
                            new-expense-date.edit-text = x.date;
                            new-expense-amount.edit-text = x.amount;
                            new-expense-type.current-value = x.expense-type;
                            new-expense-subtype.current-value = x.expense-subtype;
                        }
                        delete => {
                            ExpenseData.selected-expense-id = x.id;
                            expense-action = MessageType.delete;
                            selected-expense = x;

                            ExpenseData.new-expense({
                                message: expense-action,
                                id: ExpenseData.selected-expense-id,
                                description: x.description,
                                amount: x.amount,
                                date: x.date,
                                expense-type: x.expense-type,
                                expense-subtype: x.expense-subtype
                            });
                        }
                    }
                }
            }
        }
        BufferLine { 
            buffer: 10px;
            line-thickness: 3px;
        }
        Rectangle {
            new-expense := HorizontalLayout {
                out property <MessageType> current-use;
                new-expense-id := TableText {                        
                    width: 40px;
                    default-text: "";
                }
                new-expense-prop-id := TableText {
                    width: 40px;
                    default-text: "Property";
                }
                new-expense-date := TableText { 
                    width: 100px;
                    default-text: "Date";
                }
                new-expense-description := TableText { 
                    width: 150px;
                    default-text: "Description";
                }
                Rectangle {
                    width: 150px;
                    new-expense-type := ComboBox {
                        width: parent.width;
                        model: [
                            "Select Expense Type", 
                            "Utilities", 
                            "Maintenance", 
                            "Other"
                        ];
                        current-index: 0;
                    }
                }
                Rectangle {
                    width: 150px;
                    new-expense-subtype := ComboBox {
                        width: parent.width;
                        model: [""];
                        current-index: 0;
                        enabled: true;
                        states [
                            Utility when new-expense-type.current-value == "Utilities": {
                                enabled: true;
                                new-expense-subtype.model: [
                                    "Water",
                                    "Electricity",
                                    "Garbage",
                                    "Gas",
                                    "Other"
                                ];
                            }
                            Maintenance when new-expense-type.current-value == "Maintenance": {
                                enabled: true;
                                new-expense-subtype.model: [
                                    "Repairs",
                                    "Cleaning",
                                    "Landscaping",
                                    "Other"
                                ];
                            }
                            None when new-expense-type.current-value != "Utilities" && new-expense-type.current-value != "Maintenance" : {
                                new-expense-subtype.enabled : false;
                            }
                        ]
                    }                        
                }
                new-expense-amount := TableText { 
                    width: 70px;
                    default-text: "Amount";
                }
                new-expense-action-icons := CreationActionIcons {
                    width: 70px;
                    confirm => {
                        
                        ExpenseData.new-expense({
                            message: new-expense.current-use,
                            id: new-expense.current-use == MessageType.update ?
                                selected-expense.id :
                                Validation.get-valid-id(IdType.Expense),
                            prop-id: new-expense-prop-id.edit-text.to-float(),
                            description: new-expense-description.edit-text,
                            amount: new-expense-amount.edit-text.to-float(),
                            date: new-expense-date.edit-text,
                            expense-type: new-expense-type.current-value,
                            expense-subtype: new-expense-subtype.current-value
                        });
                    }
                    cancel => {
                        new-expense.current-use = MessageType.create;

                        new-expense-id.clear();
                        new-expense-prop-id.clear();
                        new-expense-description.clear();
                        new-expense-amount.clear();
                        new-expense-date.clear();
                        new-expense-type.current-value = "Select Expense Type";
                        new-expense-subtype.current-index = 0;
                    }
                }
            }
        }
        Rectangle {
            height: 10px;
        }
    }
}

export component AddExpenseMenu inherits Rectangle {
    public function clear-expense-menu() {
        description-input.text = "";
        amount-input.text = "";
        date-input.text = "";
    
        expense-type.current-value = "Select Expense Type";

        expense-action = MessageType.create;
    }

    public function open-expense(expense: ExpenseInput) {
        description-input.text = expense.description;
        amount-input.text = expense.amount;
        date-input.text = expense.date;
        expense-type.current-value = expense.expense-type;
        subtype.current-value = expense.expense-subtype;

        ExpenseData.selected-expense-id = expense.id;
        expense-action = MessageType.update;
    }
    
    public function submit-expense() {
        ExpenseData.potential-expense-id = Validation.get-valid-id(IdType.Expense);
        ExpenseData.new-expense({
            message: expense-action,
            id: expense-action == MessageType.update ? 
                        ExpenseData.selected-expense-id : 
                        expense-action == MessageType.delete ? 
                        ExpenseData.selected-expense-id : 
                        ExpenseData.potential-expense-id,
            description: description-input.text,
            amount: amount-input.text.to-float(),
            date: date-input.text,
            expense-type: expense-type.current-value,
            expense-subtype: subtype.current-value
        });
        clear-expense-menu();
    }
    in-out property <string> expense-subtype-string;
    in-out property <MessageType> expense-action;
    
    border-width: 2px;
    border-radius: 5px;
    border-color: black;
    padding: 10px;

    HorizontalLayout {
        GridBox {
            spacing: 20px;
            padding: 10px;
            y: 10px;
            //preferred-width: parent.width / 2;
            //max-width: parent.max-width;
            height: 50px;
            Text {
                text: "Add expenses";
                col: 0;
                row: 0;
            }
            description-input := LineEdit {
                col: 0;
                row: 1;
                placeholder-text: "Description";
                input-type: InputType.text;
            }
            amount-input := LineEdit {
                col: 1;
                row: 1;
                placeholder-text: "Amount";
                input-type: InputType.decimal;
            }
            date-input := LineEdit {
                col: 2;
                row: 1;
                placeholder-text: "Date";
                input-type: InputType.number;
            }
            Rectangle {
                col: 0;
                row: 2;
                min-width: 100px;
                preferred-width: 150px;
                padding: 5px;
                HorizontalLayout {
                    alignment: LayoutAlignment.start;
                    //max-width: 200px;
                    min-width: 100px;
                    preferred-width: 150px;
                    spacing: 100px;
                    padding: 20px;
                    Rectangle {
                        expense-type := ComboBox {
                            model: [
                                "Select Expense Type", 
                                "Utility", 
                                "Maintenance", 
                                "Other"
                            ];
                        }
                    }
                    Rectangle {
                        subtype := ComboBox {
                            model: [""];
                            enabled: true;
                            states [
                                Utility when expense-type.current-value == "Utility": {
                                    enabled: true;
                                    subtype.model: [
                                        "Water",
                                        "Electricity",
                                        "Garbage",
                                        "Gas",
                                        "Other"
                                    ];
                                }
                                Maintenance when expense-type.current-value == "Maintenance": {
                                    enabled: true;
                                    subtype.model: [
                                        "Repairs",
                                        "Cleaning",
                                        "Landscaping",
                                        "Other"
                                    ];
                                }
                                None when expense-type.current-value != "Utility" && expense-type.current-value != "Maintenance" : {
                                    subtype.enabled : false;
                                }
                            ]
                        }                        
                    }
                }
            }
            Rectangle {
                col: 0;
                row: 3;
                HorizontalLayout {
                    StandardButton {
                        kind: ok;
                        clicked => {
                            submit-expense();
                            clear-expense-menu();
                        }
                    }
                    StandardButton {
                        kind: cancel;
                        clicked => {
                            clear-expense-menu();
                        }
                    }
                    Button {                        
                        enabled: expense-action == MessageType.update ? true : false;
                        text: "Delete";
                        clicked => {
                            expense-action = MessageType.delete;
                            submit-expense();
                        }
                    }
                }
            }
        } 
    }
}

export component ExpenseMenu inherits Rectangle {
    HorizontalLayout {
        /*
        Rectangle {
            min-width: 100px;
            max-width: 300px;
            expense-history := Rectangle {
                y: 10px;
                border-radius: 2px;
                border-color: black;
                VerticalLayout {
                    Rectangle {
                        Text {
                            font-size: 14px;
                            text: "Expense History";
                        }
                    }
                    BufferLine {
                        buffer: 10px;
                        line-thickness: 2px;
                    }
                    ListView {
                        padding: 20px;
                        for x in ExpenseData.expenses: expense-button := TouchArea {
                            ta-bg := Rectangle {
                                background: expense-button.has-hover? cornsilk : root.background;
                                Text {
                                    horizontal-alignment: TextHorizontalAlignment.left;
                                    text: x.id + ". " + x.description;
                                }
                            }
                            clicked => {
                                ExpenseData.selected-expense-id = x.id;
                                add-expense-menu.open-expense(x);
                            }
                        }
                    }
                }
            }
        }
        */
        Rectangle {
            min-width: 400px;
            preferred-width: 600px;
            max-width: 1000px;
            property <ExpenseMenuState> current-state: ExpenseMenuState.totals;
            VerticalLayout {
                states [
                    View when current-state == ExpenseMenuState.view : {
                        view-totals-menu.visible : false;
                        main-view-menu.visible : true;
                    }
                    Totals when current-state == ExpenseMenuState.totals : {
                        view-totals-menu.visible : true;
                        main-view-menu.visible : false;
                    }
                ]
                HorizontalLayout {
                    Button {
                        text: "Expenses";
                        clicked => {
                            current-state = ExpenseMenuState.totals;
                        }
                    }
                    Button {
                        text: "Visualizations";
                        clicked => {
                            current-state = ExpenseMenuState.view;
                        }
                    }
                }
                Rectangle {
                    main-view-menu := MainExpenseView {
                        visible: false;
                    }
                    view-totals-menu := ViewExpenseTotalsMenu {
                        visible: false;
                    }
                }                
            }
        }
    }
}